<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Todos" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <%= link_to 'Todos', root_path, class: 'brand' %>
          <p class="navbar-text" data-current-user="<%= current_user.email %>"><%= current_user.email %></p>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <% flash.keys.each do |key| %>
          <%= content_tag :div, flash[key], class: "#{key} flash" %>
        <% end %>

        <%= yield %>

      </div><!--/content-->

      <footer>
        <p>&copy; Steve McKinney 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
